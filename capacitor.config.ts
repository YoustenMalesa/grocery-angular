import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'za.co.tut.grocery',
  appName: 'angular-ecommerce',
  webDir: 'dist/angular-ecommerce',
  bundledWebRuntime: false
};

export default config;
